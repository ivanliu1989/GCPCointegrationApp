#First time
	cd to the directory
	#Create local git
	git init
	#Add all things in the current directory to the local git
	git add .
	#Add a remote repository
	git remote add https://Piyapong_4507075@bitbucket.org/Piyapong_4507075/gcgcointegration.git
	#Commit with comments
	git commit -m "first version"
	#Push to a remote repository
	git push origin master

#Other time
	cd to the directory
	cd GCP
	#Add all things in the current directory to the local git
	git add .
	#Commit with comments
	git commit -m "Initial version"
	#Push to a remote repository
	git push origin master

#Optional force push to a remote repository
git push -f origin master

#Display current status
git status

File
Go to >> ~/pong1-backup/web1/flask/
app.wsgi ==> set system path
flaskapp.py ==> init app call

Folder
~/web ==> css, js files
~/src ==> used python file
~/file ==> bash file to back up
~/test ==> unused file
